<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <%-include("./partials/header.ejs",{title: "See all your genres Here"}) %>
    <main>
      <article>
        <ol>
          <% if(genres.length > 0) { %> <% genres.map(genre => { %>

          <a href="/genres/<%=genre.id %>">
            <li data-key="<%=genre.id %>"><%= genre.name %></li>
          </a>

          <% }) %> <% } else { %>
          <h2>Create a genre</h2>
          <% } %>
        </ol>
      </article>
    </main>

    <%-include("./partials/footer.ejs") %>
  </body>
</html>
